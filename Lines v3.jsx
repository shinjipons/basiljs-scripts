#includepath "~/Documents/;%USERPROFILE%Documents";#include "basiljs/bundle/basil.js";function draw() {	b.canvasMode(b.MARGIN);	var tileCount = 50;	b.strokeWeight(1);	// diagonal lines	for (var gridY = 0; gridY <= tileCount; gridY++) {		for (var gridX = 0; gridX <= tileCount; gridX++) {			var posX = b.width/tileCount * gridX;			var posY = b.height/tileCount * gridY;			b.line(posX, posY, b.width/2, b.height/2);		}	}	var stepX = b.width/tileCount;	var stepY = b.height/tileCount;	// horizontal lines	for (var posX = 0; posX <= b.width; posX = posX + stepX){		b.line(posX, 0, posX, b.height);	}	// vertical lines	for (var posY = 0; posY <= b.height; posY = posY + stepY) {		b.line(0, posY, b.width, posY);	}};b.go(b.MODESILENT);