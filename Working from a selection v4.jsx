#includepath "~/Documents/;%USERPROFILE%Documents";#include "basiljs/bundle/basil.js";function draw() {    b.doc();        selItems = b.selections()[0];      var layer = b.layer("Layer A");    b.duplicate(selItems);    //this works, it duplicated the selected item!    //but now I need to be able to change the duplicated item    b.typo(selItems, 'appliedFont', 'Modulo 42'); //dont forget the space between Modulo and 42    //as expected, this changed the selected item but not the duplicated one!    //attaching labels to created items might help};b.go();