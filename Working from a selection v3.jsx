#includepath "~/Documents/;%USERPROFILE%Documents";#include "basiljs/bundle/basil.js";function draw() {    b.doc();        selItems = b.selections()[0];      // add new layer    var layer = b.layer("Test Layer 1");    b.ellipse(20,20,10,10);    //add another new layer    var layer = b.layer("This is another layer");    b.typo(selItems, 'appliedFont', 'Modulo 42');        //b.typo(selItems, 'appliedFont', 'Modulo 14');    //b.typo(selItems, 'appliedFont', 'Modulo 32');};b.go();